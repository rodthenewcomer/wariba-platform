{
  "name": "@wariba/test-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic clocks, ID generators, fixture builders and scenario packs for tests.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint --config ../../eslint.config.mjs .",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "seed:phase25": "tsx scripts/seed-phase25-evidence.ts"
  },
  "dependencies": {
    "@wariba/database": "workspace:*",
    "@wariba/domain": "workspace:*",
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "@wariba/application": "workspace:*",
    "tsx": "^4.23.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
