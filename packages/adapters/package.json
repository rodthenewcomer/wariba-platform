{
  "name": "@wariba/adapters",
  "version": "0.0.0",
  "private": true,
  "description": "Provider port interfaces (market data, payment, payout, email, storage, KYC, feature flags) with sandbox implementations.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint --config ../../eslint.config.mjs .",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
