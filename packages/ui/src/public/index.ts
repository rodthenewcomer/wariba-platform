/**
 * Public page primitives — Phase 3.4.5A §13, §27–§29.
 *
 * The shell's contribution to every page that comes after it: one vertical
 * rhythm, one set of section backgrounds, one colour field, one product
 * surface, one card with deliberate variants. None of them encode content.
 */
export { PublicSection, SectionHeader } from './PublicSection';
export type { PublicSectionProps, SectionHeaderProps, SectionTone } from './PublicSection';
export { StrongColorSurface, DarkProductSurface, VisualCard } from './Surfaces';
export type { StrongColorSurfaceProps, DarkProductSurfaceProps, VisualCardProps } from './Surfaces';
