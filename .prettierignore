node_modules
.turbo
dist
build
.next
coverage
supabase/.temp
pnpm-lock.yaml
*.tsbuildinfo

# WARIBA reference documents are hand-authored source of truth (AGENTS.md §2) —
# not reformatted by tooling.
docs/**
AGENTS.md

# Generated from docs/05-design/tokens.json by packages/design-tokens/scripts/build.mjs —
# machine-formatted output, not hand-edited.
packages/design-tokens/src/tokens.css
packages/design-tokens/src/tokens.generated.ts

# Playwright global-setup artefacts (session cookies + the seeded fixture),
# written on every local E2E run. Machine output, never committed — without
# this, `pnpm test:fast` fails on any machine that has run the E2E suite.
apps/web/tests/e2e/.auth
